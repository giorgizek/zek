
// ReSharper disable InconsistentNaming

namespace Zek.Cryptography
{
    /*public static class MD5Helper
    {
        public static string MD5Hex(string plainText)
        {
            using (var md5 = MD5.Create())
            {
                return ByteArrayHelper.ByteArrayToHex(md5.ComputeHash(Encoding.UTF8.GetBytes(plainText)));
            }
        }
        public static bool VerifyMD5Hex(string cypherText, string plainText)
        {
            var md5 = MD5Hex(plainText);
            return cypherText == md5;
        }
    }*/
}
